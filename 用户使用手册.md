# Alpha 多因子组合健康度守卫 — 用户使用手册

> 适用目录：`agent-alpha-portfolio-guardian`  
> 行为入口：[SKILL.md](SKILL.md)｜QuantSkills 声明：[AGENTS.md](AGENTS.md)｜规则细节：`references/decision-rules.md`  
> 样例产出：`reports/samples/mock_run/`、`reports/samples/backtest_mock/`

---

## 1. 这个 Agent 能帮你做什么

面向**已经在用的多因子组合**，做组合级健康度监控与研究建议，而不是单因子一次评估。

| 你想解决的问题 | Agent 给出的结果 |
|----------------|------------------|
| 组合里哪些因子还健康、哪些在衰减？ | **因子健康度矩阵**（评分、半衰期、状态、建议动作） |
| 有没有过热/踩踏风险需要提高警惕？ | **拥挤度警示**（挂到相关因子暴露上） |
| 该观察、降权、重构，还是移出候选池？ | **退休 / 重构 / 降权候选清单**（带原因码） |
| 各因子信号还能用多久、再平衡怎么对照？ | **IC 衰减曲线**（图 + JSON） |
| 这套守卫规则历史上靠不靠谱？ | **有效性回测**（分桶、准确率、回撤、滚动窗） |

一句话：把 evaluate、decay、拥挤盯盘、聪明钱画像四类证据编成**可阅读、可交接、可机器读取**的组合健康度报告包。

---

## 2. 不能做什么（避免误用）

- 不会下单，也不会输出「买入 / 卖出 / 必涨 / 仓位提到 xx%」一类指令  
- 不是 `alpha-*` 生产因子包，不能把会话内结果直接当交易库 parquet  
- 不会在依赖失败时编造完整数值  
- 不替代单因子评估 Skill：主分与衰减仍由 `skill-factor-evaluate` / `skill-factor-decay` 负责  

研究建议用语示例：「可关注」「建议研究降权 / 移出候选池」「拥挤度升高，需提高风险警惕」。

---

## 3. 适用人群与场景

| 角色 | 典型场景 |
|------|----------|
| 多因子研究员 | 周度/日度检查因子库健康度，筛退休与重构对象 |
| 组合经理助理 | 汇总过热与资金分歧旁证，形成观察清单 |
| 下游 Agent | 读取 `agent_snapshot.json` / 结构化表做交接 |
| 新人上手 | 先跑 mock / self-test，对照样例学会读报告 |

**适合**：已有因子信号或依赖 Skill 报告，需要组合视角持续监控。  
**不适合**：从零挖因子、要直接交易指令、只要单日大盘复盘。

---

## 4. 环境准备

### 4.1 依赖

```powershell
cd f:\ProjectGeek\skills\agent-alpha-portfolio-guardian
# 推荐 skills-env
D:\env\anaconda3\envs\skills-env\python.exe -m pip install -r requirements.txt
```

同级目录建议具备：

| 包 | 用途 |
|----|------|
| `skill-factor-evaluate` | 单因子 ScoreReport |
| `skill-factor-decay` | DecayReport / IC 曲线 |
| `agent-crowding-risk-monitor` | 拥挤 snapshot |
| `skill-smart-money-profiler` | 资金合力/分歧旁证 |

正式数据经上述 Skill（或其已落盘产物）获取，使用 Pandadata 溯源。

### 4.2 凭证（按需）

复制 `.env.example` 为环境变量或宿主密钥（勿提交真实密钥）：

```text
PANDA_DATA_USERNAME=...
PANDA_DATA_PASSWORD=...
```

- 已有依赖 Skill 报告/缓存时：本 Agent 可不持有凭证  
- 需要在线拉新且本地无产物时：再配置凭证  

---

## 5. 五分钟上手

### 方式 A：先看样例（零配置）

打开：

1. `reports/samples/mock_run/runtime_report.md` — 主报告  
2. `reports/samples/mock_run/health_matrix.csv` — 健康度矩阵  
3. `reports/samples/backtest_mock/l4.html` — 回测 L4 交互页（浏览器打开）  
4. `reports/samples/backtest_mock/backtest_report.md` — 回测文字报告  

### 方式 B：本机自测

```powershell
D:\env\anaconda3\envs\skills-env\python.exe -m runtime self-test
```

会刷新 `reports/samples/mock_run/` 与 `reports/samples/backtest_mock/`。

### 方式 C：对话触发（Cursor 等）

加载本仓库后可以说：

- 「用 alpha-portfolio-guardian 对当前多因子组合做健康度守卫」  
- 「只出因子健康度矩阵和退休候选」  
- 「结合拥挤盯盘，标出组合里过热相关的因子」  
- 「画出组合内各因子 IC 衰减曲线并对比半衰期」  
- 「对守卫规则做有效性回测」  

---

## 6. 配置你的组合

### 6.1 从样例复制

```powershell
Copy-Item config\portfolio.example.yaml config\portfolio.yaml
```

自测可用现成 `config/portfolio.mock.yaml`。

### 6.2 关键字段

| 字段 | 含义 |
|------|------|
| `as_of` | 结论锚定日；`null` 表示用运行日 |
| `universe` | 股票池（组合内因子须同一池） |
| `horizon_eval` | 评价持有期（天），组合内须一致 |
| `horizon_decay` | 衰减期限列表，默认 1/3/5/10/20 |
| `source_mode` | `live` / `mock` / `degraded` |
| `data_source` | 正式运行填 `Pandadata` |
| `factors[]` | 因子清单（见下） |
| `crowding` | 是否启用拥挤桥接、snapshot 路径 |
| `smart_money` | 是否启用聪明钱抽样、sample 路径 |
| `thresholds` | 退休/脆弱等规则阈值 |
| `rules_version` | 规则版本，进入产物版本号 |

### 6.3 单个因子怎么配

```yaml
factors:
  - factor_id: F001
    name: momentum_20
    signal_path: data/factors/F001.parquet          # 可选
    score_report_path: deps/F001_score.json          # 推荐：evaluate 产物
    decay_report_path: deps/F001_decay.json          # 推荐：decay 产物
    exposure_symbols: ["600519.SH", "000001.SZ"]   # 用于拥挤/聪明钱挂载
```

优先级简述：

1. 显式 `score_report_path` / `decay_report_path`  
2. 与信号同名的旁路 `*_score.json` / `*_decay.json`  
3. `mock` 模式：无文件时用合成证据（仅自测）  

报告 JSON 建议带 `data_source: Pandadata`；也可旁路 `{文件名}.provenance.json`（见 `references/provenance.example.json`）。

### 6.4 拥挤与聪明钱

```yaml
crowding:
  enabled: true
  themes: []
  snapshot_path: ../agent-crowding-risk-monitor/outputs/live/agent_snapshot.json

smart_money:
  enabled: true
  mode: consensus
  top_n_symbols: 10
  sample_path: null    # 可填预置合力/分歧 JSON
```

---

## 7. 命令一览

在仓库根目录执行（以下以 `python` 指你的解释器）：

| 命令 | 作用 |
|------|------|
| `python -m runtime --mock` | mock 全链路，快速出报告 |
| `python -m runtime --portfolio config/portfolio.yaml --live` | 按配置跑正式研究态 |
| `python -m runtime self-test` | 规则单测 + mock + 回测样例刷新 |
| `python -m runtime validate <run_dir>` | 校验某次研究态产物 |
| `python -m runtime publish --from <run_dir>` | 验收通过后写发布快照 |
| `python -m runtime backtest --allow-simulate` | 守卫规则有效性回测（模拟） |
| `python -m runtime backtest --metrics-panel a.csv --fwd-panel b.csv` | 用历史面板回测 |
| `python -m runtime clean` | 清理 `reports/runtime_out/` |

可选：

```text
python -m runtime --portfolio config/portfolio.yaml --live --output-dir reports/runtime_out/my_run
python -m runtime backtest --start_date 2024-01-01 --end_date 2025-12-31 --rebalance-days 20 --horizon 20
```

---

## 8. 标准工作流（推荐）

```text
1. 用 evaluate / decay（及可选 crowding、smart-money）准备各因子报告或快照
2. 填写 config/portfolio.yaml，挂上路径与暴露标的
3. python -m runtime --portfolio config/portfolio.yaml --live
4. 阅读 runtime_report.md + health_matrix.csv
5. python -m runtime validate reports/runtime_out/<run_id>
6. 需要固化给下游时：python -m runtime publish --from ...
7. 需要检验规则时：python -m runtime backtest --metrics-panel ...（或 --allow-simulate）
```

对话编排时，宿主 Agent 应按同样顺序：先读依赖 Skill 文档 → 取证 → 再跑本 Agent。

---

## 9. 如何读产出

### 9.1 研究态目录

`reports/runtime_out/<run_id>/`

| 文件 | 你拿它做什么 |
|------|----------------|
| `runtime_report.md` | 主报告：L1 结论 + 矩阵 + 警示 + 候选 + 衰减 |
| `health_matrix.csv` / `.parquet` | 机器可读健康度表 |
| `crowding_alerts.json` | 拥挤警示列表 |
| `retire_rebuild_candidates.csv` | 动作候选（retire / rebuild / deweight） |
| `ic_decay_curves.json` + `charts/` | 衰减曲线数据与图 |
| `agent_snapshot.json` | 给下游 Agent 的摘要入口 |
| `handoff_card.md` | 交接卡 |
| `run_summary.json` / `validation.json` | 运行摘要与门禁结果 |
| `deps/` | 本次用到的依赖产物索引 |

### 9.2 健康度矩阵怎么读

关注列：`primary_score`、`half_life`、`crowding_level`、`capital_consensus`、`health_status`、`signal`、`gap_notes`、`reasons`。

常见 `signal`：

| signal | 含义（研究建议） |
|--------|------------------|
| `keep` | 当前证据支持保留观察 |
| `watch` | 进入观察带 |
| `deweight` | 建议研究降权 |
| `retire_candidate` | 建议研究移出候选池 |
| `rebuild_candidate` | 定义/期限或许可重构后再评 |
| `insufficient` | 证据不足，看 `gap_notes` |

原因码释义见 [references/decision-rules.md](references/decision-rules.md)。

### 9.3 发布态

```text
python -m runtime publish --from reports/runtime_out/<run_id>
```

写入：`reports/publish/<as_of>/<data_version>/`  
下游默认读发布态；需要重算请显式再跑研究态。

### 9.4 回测产物

`reports/backtest/<run_id>/`（样例：`reports/samples/backtest_mock/`）

| 文件 | 内容 |
|------|------|
| `l4.html` | **SCI 风格交互页**（内嵌数据，浏览器直接打开） |
| `backtest_report.md` | 可读总结 |
| `bucket_stats.csv` | 各 signal 后续 fwd_ic / fwd_ret |
| `accuracy.json` | 描述性准确率及定义 |
| `rolling_oos.csv` | 滚动窗样本外 |
| `keep_equity.csv` | keep 组合代理净值路径 |
| `summary.json` | 机器可读总览 |

回测结束后会自动写出 `l4.html`。也可对已有目录单独导出：

```powershell
python scripts/export_l4_backtest.py --run-dir reports/backtest/<run_id>
```

浏览器打开该文件即可；样式对齐 `agent-macro-driven-rotation` 的 L4 科研绘图风格。

这是**守卫规则**有效性，不是 Alpha 生产全套 IC 回测。

---

## 10. 在对话里怎么用（场景剧本）

### 场景 1：周度组合体检

> 用 agent-alpha-portfolio-guardian，读取我挂好的 ScoreReport/DecayReport，对组合做健康度守卫，给出矩阵和退休/重构候选。

你应检查：`runtime_report.md` 的 L1、候选清单、`gap_notes`。

### 场景 2：只想看谁该退

> 只要因子健康度矩阵和退休候选，不要长文。

可重点打开 `health_matrix.csv`、`retire_rebuild_candidates.csv`。

### 场景 3：担心风格/主题过热

> 结合 crowding 的 snapshot，标出和拥挤相关的因子。

确认 `portfolio.yaml` 里 `crowding.snapshot_path` 或能找到默认 snapshot；看 `crowding_alerts.json`。

### 场景 4：解释「为什么降权」

> 对 Top 暴露标的补聪明钱合力/分歧，解释列入降权的旁证。

旁证不改主分，只影响谨慎程度；席位标签为规则匹配，非官方认定。

### 场景 5：检验规则是否靠谱

> 对当前 rules_version 做守卫信号有效性回测。

有历史面板用 `--metrics-panel`；没有时可用 `--allow-simulate` 做方法演练（结果标为模拟）。

---

## 11. 阈值与规则（可调）

默认见 `portfolio` 的 `thresholds`：

| 键 | 默认 | 作用 |
|----|------|------|
| `half_life_healthy` | 5 | 半衰期达到则偏健康 |
| `half_life_fragile` | 2 | 低于则偏脆弱 |
| `primary_score_retire` | -0.5 | 主分不高于此考虑退休/重构 |
| `ic_noise_abs` | 0.005 | \|IC\| 低于此视噪声 |
| `high_turnover` | 0.6 | 高换手阈值 |

修改阈值后务必更新 `rules_version`，以便产物可追溯。

---

## 12. 常见问题

**Q：一跑 live 就失败？**  
A：检查是否声明了 `data_source: Pandadata`，以及是否已有带溯源的依赖产物；若既无产物又要在线取数，需配置凭证。

**Q：矩阵全是 `insufficient`？**  
A：缺少 ScoreReport/DecayReport。先跑 evaluate/decay，或把路径写进 `portfolio.yaml`。

**Q：mock 和 live 有什么区别？**  
A：mock 用合成证据做流程自测；live/degraded 才对应正式研究结论。版本号前缀不同，勿混用。

**Q：能当交易信号吗？**  
A：不能。输出是研究建议与候选清单，交易决策与执行在你自己的系统内完成。

**Q：回测用真实数据怎么接？**  
A：准备历史证据面板 CSV/Parquet（含 date、factor_id、primary_score、rank_ic、half_life 等）及后验 `fwd_ic`/`fwd_ret`，用 `--metrics-panel` / `--fwd-panel`。

**Q：图表没出来？**  
A：看 `charts/ic_decay_family.png`；若环境无 matplotlib，会有 ASCII 降级文件。

---

## 13. 目录速查

```text
agent-alpha-portfolio-guardian/
├── AGENTS.md                ← QuantSkills Agent 声明
├── SKILL.md                 ← Agent 行为入口
├── README.md / README.en.md
├── LICENSE                  ← GPL-3.0-only
├── 用户使用手册.md          ← 本文
├── config/
│   ├── portfolio.example.yaml
│   └── portfolio.mock.yaml
├── runtime/                 ← python -m runtime
├── scripts/                 ← 桥接、校验、回测、L4 导出
├── templates/               ← 报告模板、l4_backtest.html
├── references/              ← 规则、数据、边界
├── reports/
│   ├── samples/             ← 结构样例（可提交）
│   ├── runtime_out/         ← 研究态（gitignore）
│   ├── publish/             ← 发布态（gitignore）
│   └── backtest/            ← 回测产物（gitignore）
└── agents/                  ← Cursor / OpenAI 等宿主适配
```

---

## 14. 免责声明

本手册与 Agent 产出仅供量化研究与工作流参考，不构成任何投资建议。是否用于实盘或生产链路，由使用者自行判断并承担责任。
